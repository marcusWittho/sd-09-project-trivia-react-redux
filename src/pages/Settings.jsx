import React from 'react';

class Settings extends Settings.Component {
  render() {
    return (
      <div>
        <h2 data-testid="settings-title">Settings</h2>
      </div>
    );
  }
}

export default Settings;
