import React, { Component } from 'react';
import Header from '../components/Header';

class Game extends Component {
  render() {
    return (
      <section>
        <Header />
      </section>
    );
  }
}

export default Game;
